{
  "name": "ipcow-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Privacy-first IP, DNS & email tooling — the MIT core behind ipcow.com",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
